package enums

// ─── Setting Keys ────────────────────────────────────────────────────

const (
	// spritesheet_config = {enabled, slotRate} — shared with the
	// vdohide-service enqueuer; worker reads only .enabled as a kill switch
	SettingSpritesheetConfig = "spritesheet_config"
)
